# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

# environment: production
title: APK Systembolaget
tagline: Jämför APK för systembolagets produkter
email: e@mrik.ga
description: >- # this means to ignore newlines until "baseurl:"
  Systembolagets produkter sorterade efter APK mätt i milliliter etanol per krona.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://alkolist.github.io/"
locale: sv_SE
github_username:  C4illin
google_site_verification: 7k99U4eYzeJ8a4n0QWBnT9CM7sOwcDad7s4YBrlprI8
social:
  name: C4illin
  links:
    - https://github.com/C4illin

# Build settings
# theme: minima
plugins:
  - jekyll-get-json
  - jekyll-sitemap
  - jekyll-seo-tag

jekyll_get_json:
  - data: apk
    json: 'https://raw.githubusercontent.com/C4illin/systembolaget-data/main/apksort.json'

# assets:
#   source_maps: true
#   destination: "/assets"
#   compression: true
#   gzip: false
#   defaults:
#     integrity:
#       {css,img,js}: false
#   caching:
#     enabled: true
#     path: ".jekyll-cache/assets"
#     type: file

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - node_modules/
  - yarn.lock
  - package.json
  - README.md
  - Gemfile
  - Gemfile.lock
  - old/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - gemfiles/
  - .jekyll-cache/
#   - .sass-cache/
#   - node_modules/

